# Agent Workbench MVP PRD

> 工作名称：Agent Workbench（可替换）  
> 版本：MVP v0.1  
> 状态：首稿  
> 目标用户：同时运行多个 Claude Code 与终端任务的个人开发者

## 1. 一句话定义

一个网页化的 AI 终端工作台：用户可以创建互不干扰的终端或 Claude Code session，并在不逐个翻看终端的情况下，立刻知道 **哪个任务正在推进、哪个需要自己的决定、以及下一步该做什么**。

## 2. 核心价值与产品原则

### 核心价值

用户的痛点不是无法同时开多个终端，而是任务一多后，无法快速回答：

- 哪个 Claude 已经完成，结果是否值得我审核？
- 哪个 Claude 正在等我回答；我需要在什么选项之间做选择？
- 哪个任务卡住、失败或已没有进展？
- 我离开 20 分钟后，应该先处理什么？

产品的核心承诺：**将多条长终端记录压缩为一组可行动的工作摘要（Actionable Briefs）。**

### 产品原则

1. **管理优先于监看**：首页默认是 Attention Inbox，不是十宫格终端墙。
2. **语义优先于颜色**：状态必须说明“为什么需要你”，不能只显示 Running/Done。
3. **事实与推断分开**：系统观测、Agent 上报、AI 归纳必须标示来源。
4. **只在必要时打扰**：完成不等于报警；只有决策、权限和严重阻塞才主动通知。
5. **终端不被替代**：用户随时可进入完整终端；摘要负责让用户决定“要不要进去”。
6. **隔离是默认值**：并行 Claude 写代码时默认独立 Git worktree；不可把多个任务放进同一工作副本。

## 3. 目标、非目标与假设

### MVP 目标

- 在一个网页中创建、查看和继续多个持久 session。
- 同时支持普通 shell 与 Claude Code。
- 为 Claude session 显示当前进度、已完成事项、待决策事项、推荐下一步。
- 当 Claude 完成、失败、请求权限或等待用户回答时，将对应 session 提升到 Attention Inbox 并通知用户。
- 浏览器刷新、切换标签或短暂断线不终止 session。

### MVP 非目标

- 不做完整 IDE、代码编辑器或 GitHub PR 审阅平台。
- 不在 MVP 支持多人实时协同、组织权限层级或计费体系。
- 不承诺自动理解任意普通 shell 的“业务进度”。
- 不让 AI 摘要自动执行命令、批准权限或代替用户做高风险选择。

### 关键假设

- 首个版本服务单用户、本机或用户自控服务器。
- 首要场景是 3–10 个并行任务，而非数百个 worker。
- Claude Code 是第一个深度集成对象；普通终端仅提供可靠的会话管理与基础观测。

## 4. 用户与核心场景

### 用户画像

独立开发者或技术负责人，同时推进 bug 修复、功能开发、研究、测试和部署准备；习惯使用 Claude Code，但不愿把注意力耗在切换终端和回忆上下文上。

### 核心场景

#### 场景 A：并行委派

用户创建三个 Claude session：修复登录 Bug、补测试、调研数据库迁移方案。每条任务各自使用独立 worktree。用户继续工作，不需要盯住终端。

#### 场景 B：被需要时才介入

迁移方案 agent 发现两个可行方案。session 进入 `needs_decision`，卡片常驻 Attention Inbox，显示推荐方案、理由和两个可点击选项。用户点击选项后，答案被送回原 session。

#### 场景 C：离开后快速恢复全局认知

用户半小时后回到网页，不读聊天历史，只看 Inbox：一个完成待审、一个测试失败、一个正等待数据库窗口选择。用户按优先级处理三个卡片。

#### 场景 D：普通终端共存

用户另开一个 shell 跑开发服务器。它显示运行状态、路径和最近输出，但不假装知道“功能是否完成”。用户可写一句手工备注。

## 5. 信息架构与主交互

### 页面结构

1. **Attention Inbox（默认首页）**
   - Needs decision / Needs permission / Blocked / Review ready 四个分组。
   - 每张卡片是一个可行动摘要，点击进入 session。
2. **All Sessions**
   - 按项目、状态、类型、最近活动筛选。
   - 用于查看全部运行、已完成和普通终端。
3. **Session Workspace**
   - 左侧：session 元数据与 Brief。
   - 主区：完整可交互网页终端。
   - 右侧：事件时间线、决策历史与快速操作。

### Session 卡片

卡片最小信息：名称、项目、状态、最近更新时间、一个状态句。

鼠标悬停（或键盘聚焦）后在 300–500ms 内显示 Brief：

```text
修复支付回调重复扣款                       需要决策

目标：避免 Stripe webhook 被重复处理。
进度：3/4，已完成定位与幂等键实现；正在补回归测试。
需要你决定：幂等窗口采用 24 小时还是 7 天？
推荐：7 天；覆盖延迟重试，存储成本可控。
下一步：确认后执行 migration、测试并提交 PR。
证据：Agent 上报 · 2 分钟前
```

Brief 约束：

- 最多 120 个中文字符的摘要主体；需要更多内容时进入详情页。
- `需要你决定` 必须为具体问题，而不是“请查看日志”。
- `下一步` 必须可执行，必要时提供按钮。
- 标注来源：`系统观测`、`Agent 上报` 或 `AI 归纳`。

### 状态与通知规则

| 状态 | 含义 | 页面表现 | 通知 |
|---|---|---|---|
| executing | 正在执行当前阶段 | 蓝色活动点 | 不通知 |
| verifying | 正在测试或验证 | 紫色活动点 | 不通知 |
| needs_decision | 缺少产品/技术选择 | 黄色呼吸高亮，置顶 Inbox | 立即一次 |
| needs_permission | 需要批准敏感操作 | 橙红高亮，置顶 Inbox | 立即一次，高优先级 |
| blocked | 遇到错误、依赖或凭据问题 | 橙色高亮 | 立即一次 |
| review_ready | 已完成、等待审核 | 绿色徽标 | 可配置；默认不弹窗 |
| completed | 已结束且无后续行动 | 归档 | 不通知 |
| stale | 长时间无输出但未结束 | 灰橙提示 | 默认不通知 |
| terminal_only | 普通终端，语义未知 | 灰色 | 仅进程退出时可选通知 |

通知去重：同一 session、同一 attention reason 在用户处理前只通知一次；用户回复或状态变化后解除去重。

## 6. 功能需求

### P0：Session 生命周期

- 创建 session：选择 `Terminal` 或 `Claude Code`、项目目录、名称、初始命令。
- Claude Code session 默认创建独立 Git worktree；用户可显式关闭隔离并看到风险提示。
- session 在浏览器断开时继续运行；可重新连接。
- 支持停止、重启、重命名、归档和删除 session 记录。
- Session 类型、项目目录、worktree、分支、创建时间和最后活动时间可见。

### P0：网页终端

- 支持输入、输出、复制、粘贴、resize、滚动回看和重连。
- 一个 session 对应一个持久 PTY / tmux worker；不得因切换网页 Tab 而重启。
- 多个浏览器标签只能有一个输入控制者；其他标签为只读观察，避免双重输入。

### P0：Claude Code 状态采集

- 平台以后台 session 方式启动 Claude Code，周期性读取 `claude agents --json --all`。
- 采集 `working`、`blocked`、`done`、`failed`、`stopped` 等确定性状态。
- 配置 Hook 接收权限请求、通知、任务完成、子 agent 完成、工具失败和本轮结束事件。
- 不依赖解析终端颜色、光标位置或屏幕文字来判断状态。

### P0：Actionable Brief

- 每个 Claude session 有一份结构化状态对象：

```json
{
  "objective": "用户目标",
  "phase": "executing|verifying|waiting|review",
  "progress": { "done": 2, "total": 4 },
  "completed": ["已完成事项"],
  "blocker": "可选",
  "decision": {
    "question": "可选",
    "options": ["可选"],
    "recommended": "可选",
    "reason": "可选"
  },
  "next_action": "下一步",
  "evidence": ["测试/文件/PR"],
  "source": "observed|agent_reported|ai_inferred",
  "updated_at": "ISO-8601"
}
```

- 仅在任务启动、阶段变化、出现阻塞/决策、测试结束和任务结束时刷新摘要。
- Claude 的最终消息、Hook 事件和任务初始说明是摘要的主要输入；模型不应每秒重读完整 transcript。
- 用户可点击“刷新摘要”，并可反馈“摘要不准确”。

### P0：决策与继续

- 详情页展示待决策问题、推荐项与理由。
- 用户可以输入自由文本或点击预设选项，内容写回原 Claude session。
- 每次答案记录为 decision timeline，包含问题、答案、时间与发送结果。
- 若 agent 仅是普通终端，平台只允许向终端输入，不将其标为“已理解决策”。

### P1：项目视图与通知偏好

- 按项目聚合 session，并显示项目级待处理数。
- 支持浏览器桌面通知；后续接入手机 Push。
- 用户可选择“任务完成也通知”或仅通知需行动事件。

## 7. Claude Code 集成约定

### 状态来源优先级

1. **确定性系统状态**：CLI JSON、进程是否存活、退出码、Hook 事件。
2. **Agent 上报**：Claude 调用平台提供的 `report_status` 工具，显式上报阶段、成果、阻塞和问题。
3. **AI 归纳**：根据最近事件和最后回复生成结构化 Brief。

界面必须展示最高优先级来源，避免 AI 归纳覆盖 agent 已明确上报的结论。

### 推荐的 Agent 协议

平台启动 Claude 时附加如下职责：

- 任务开始时上报计划及验收标准。
- 每完成一个阶段上报一次。
- 出现不可逆操作、需求歧义、生产操作、测试无法判断的问题时，创建 `needs_decision` 或 `blocked`。
- 任务结束时上报交付物、验证证据、风险与建议下一步。

`report_status` 可先由本地 MCP server 提供；MVP 也可由 Hook + 固定提示词实现。

## 8. 技术方案（MVP）

### 架构

```text
Browser (React + xterm.js)
        ↕ WebSocket
API / Session Broker
 ├── Session runtime：PTY 或 tmux worker
 ├── Claude adapter：CLI JSON 轮询 + Hooks HTTP 接收
 ├── Brief engine：事件归并 + 结构化摘要
 ├── Attention engine：状态机、排序、通知去重
 └── Storage：session、event、brief、decision timeline
        ↕
Shell / Claude Code / Git worktree
```

### 隔离策略

| 级别 | 方式 | 适用场景 |
|---|---|---|
| 逻辑隔离 | 独立 PTY、cwd、环境变量 | 普通本地终端 |
| 代码隔离 | 独立 Git worktree | 同一 repo 的并行 Claude 开发 |
| 执行隔离 | Container / dev container / VM | 多用户、不可信代码、端口/依赖冲突 |

MVP 使用前两级；容器隔离作为后续版本能力。

### 安全要求

- 默认只绑定 localhost；远程访问需经过可信网络或明确认证。
- 终端输入、transcript、源码路径与 Hook payload 均视为敏感数据。
- 摘要模型输入遵循最小化原则；默认仅传最近事件和必要片段。
- 摘要文本永不自动转为 shell 命令或权限批准。
- 记录所有“用户向 session 发送输入”与“权限/决策操作”。

## 9. 成功指标

### 北极星指标

**用户从打开首页到处理最需要自己介入的 session 的中位时间。**

目标：MVP 可用后，3–10 个并行 session 场景下小于 30 秒。

### 辅助指标

- `needs_decision` 出现后 10 分钟内被查看或回复的比例。
- Brief 被打开后直接执行操作（回复/批准/查看终端）的比例。
- 用户在 24 小时内打开 session 全文 transcript 的次数下降比例。
- 摘要被标记“不准确”的比例。
- session 重连成功率与浏览器刷新后的存活率。

## 10. MVP 验收标准

1. 用户可以同时创建至少 5 条 session，其中至少 3 条 Claude Code session。
2. 每条 Claude session 使用独立 worktree，文件改动不会写入其他 session 的 worktree。
3. 用户刷新浏览器后，原 session 保持运行并可继续交互。
4. Claude 等待回答、权限请求、失败和完成时，页面状态在 5 秒内更新。
5. 对一个带明确任务目标的 Claude session，悬停卡片能展示目标、当前阶段、已完成、是否需要用户与下一步；摘要更新时间不超过 2 分钟。
6. 当出现待决策问题时，用户能在网页提交答案，且 Claude 在原会话中收到该答案。
7. 普通终端不会被错误标记为“任务完成”或“需要产品决策”。

## 11. 明确不做与后续路线

### MVP 后再做

- PR / diff 审阅与一键合并。
- 多人共享、角色权限、审计导出。
- 容器/云端 worker 调度。
- 成本、token、上下文窗口和模型策略管理。
- 跨 session 交接上下文与自动编排 pipeline。
- Slack、Telegram、移动端推送。

### 需要在开工前确认的产品决策

1. MVP 是纯本地应用，还是可部署到个人服务器后远程访问？
2. 是否允许用户在网页中输入任意 shell 命令，还是只把它作为个人开发工具？
3. Claude 摘要使用用户已有 Claude Code 账号、API Key，还是平台自己的模型调用？
4. Worktree 创建失败或项目非 Git 仓库时，是否拒绝并行 Claude 写入，还是允许用户显式承担冲突风险？

## 12. 结论

MVP 的价值不在“网页开 terminal”，而在于把 terminal 变为可管理的工作单元。只要用户每次回来都能迅速知道：**哪件事已经完成、哪件事需要我、我应该怎么回应**，产品就实现了核心价值。

